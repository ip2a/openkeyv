# openkeyv

Async key-value store with a pluggable Rust core and Python bindings.

## Rust

```toml
[dependencies]
openkeyv = "0.1.5"
```

## Python

```bash
pip install openkeyv
```

The Python wheel ships Memory, Simple, Disk, SQLite, Redis, and Valkey backends.
